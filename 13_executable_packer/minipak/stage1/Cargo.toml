[package]
name = "stage1"
version = "0.1.0"
edition = "2024"

[dependencies]
encore = { path = "../encore" }
pixie = { path = "../pixie" }
lz4_flex = { version = "0.12.0", default-features = false, features = ["safe-encode", "safe-decode"] }