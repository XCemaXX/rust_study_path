[workspace]
resolver = "2"
members = [
    "encore", "minipak", "pixie", "stage1",
]

[profile.dev]
panic = "abort"

[profile.release]
debug = true
lto = "thin"
panic = "abort"

[profile.embed]
inherits = "release"
strip = "symbols"
