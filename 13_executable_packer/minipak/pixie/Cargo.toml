[package]
name = "pixie"
version = "0.1.0"
edition = "2024"

[dependencies]
deku = { version = "0.20.2", default-features = false, features = ["alloc"] }
encore = { path = "../encore" }
derive_more = { version = "2.1.1", default-features = false, features = ["display", "debug"] }